Gem::Specification.new do |spec|
  spec.name = "ion-parse-sql-flow"
  spec.version = "0.1.0"
  spec.summary = "Implement a Ruby parsers project for sql storage recovery, using log and snapshot fixtures and replay consistency checks."
  spec.files = Dir["lib/**/*.rb"]
  spec.required_ruby_version = ">= 3.0"
end
